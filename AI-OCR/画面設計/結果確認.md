# 結果確認画面 パターン比較

AI-OCR 製品(ABBYY / UiPath / Azure Document Intelligence /
Google Document AI / DX Suite など)を調査すると、結果確認画面には
共通して以下の情報が必要となる。

最近の AI-OCR では:

```text
「OCR結果を見る」
```

だけではなく、

```text
「OCR結果を確認・修正・承認・管理する」
```

画面になっている。

---

# 凡例

各セクションで使う見出しの意味:

- **必須情報**
  - 結果確認画面に存在する/載せるべき項目
- **関連実装(他画面)**
  - 結果確認画面外で既に実装されている関連機能(参考用)
- **将来要件**
  - 結果確認画面に追加する予定/検討中の機能
- **注意事項**
  - 実装上の制約・設計上の前提・既知の挙動など、画面利用時の留意点

---

# 結果確認画面に必要な画面情報

---

## 1. 処理済みファイル一覧(最重要)

### 必須情報

- ファイル名
- 処理日時
- ジョブステータスバッジ
  - `queued` / `running` / `succeeded` / `failed` / `canceled` を色分け
- レビュー状態バッジ
  - `未確認` / `完了` / `修正完了`
- 検索ボックス
  - ファイル名と ReportId で絞り込み
- フィルタパネル
  - ジョブステータス / レビュー状態 / 日付範囲(From/To)
  - 日付の自動入れ替え対応
  - フィルタ件数バッジで絞り込み状態を表示

### 将来要件

- OCR エンジンの表示
- 帳票種別の表示
  - テンプレート側に `category` フィールド追加が前提
- 信頼度低下件数(ジョブ単位の集計)
- 担当者の表示・絞り込み・割当
  - ユーザー識別の仕組みが現状なし
- 帳票種別フィルタ
- 低信頼度帳票の優先表示
- 大量履歴向けの仮想スクロール

---

## 2. 原本画像表示(中核)

### 必須情報

- PDF / 画像表示
  - `ImageDisplayViewModel` 経由で読み込み
- ページ切替
  - 前/次ボタン、`←` / `→` / `PageUp` / `PageDown` キー
- 拡大縮小
  - `Ctrl++` / `Ctrl+-` で 0.1〜5.0、ステップ 0.1
- バウンディングボックス表示
  - `ItemsControl` + Canvas オーバーレイで bbox を描画
  - 正規化座標を実座標に変換
- 選択項目ハイライト
  - テキストブロック ↔ bbox の双方向連動
  - bbox クリックで対応テキストブロックへスクロール
- 低信頼度強調
  - confidence 別の色分け
    - 高 (≥0.9): 緑 / 中 (≥0.7): 黄 / 低 (<0.7): 赤 / 不明: 青

### 将来要件

- 回転
- Fit Width / Fit Page の差別化
  - 現状は両コマンドとも `ZoomLevel=1.0` で実質同じ
- マルチ選択での領域比較
- 領域選択での手動再 OCR

---

## 3. OCR 結果編集エリア

### 必須情報

- テキストブロック表示(右ペイン ListBox)
- インライン編集
  - ダブルクリック / コンテキストメニュー「編集」で開始
  - Enter で確定、Esc でキャンセル
- 信頼度表示(パーセント形式)
- 編集マーカー(`*` HasChanges)
- Undo / Redo
- レビュー状態切替
  - 確認済み / 未確認に戻す
  - `CompletedWithEdit` のときは戻す前に確認ダイアログ
- 一括完了
  - 現ページの未確認ブロックをすべて確認済化
- 自動保存
  - レビュー状態変更時に API へ即時 PATCH
- 空白文字のみのバリデーション
- コンテキストメニュー
  - コピー / 編集 / 辞書に登録

### 関連実装(他画面)

- レビュー状態は `IReviewStateGatewayPort` 経由で永続化
- 辞書登録は辞書管理画面と連動

### 将来要件

- 項目名・ラベル表示
  - テキストブロックは現状「項目構造」を持たない(プレーンテキスト + bbox)
  - テンプレート連動で項目化する場合はモデル拡張が必要
- 必須項目表示
- 入力種別 (テキスト / 数値 / 日付 / 金額)
  - 種別ごとのバリデーション(Regex 等)も併せて検討
- 次の未確認へ移動 (ページ跨ぎのショートカット)
- 編集前後の差分表示
- 編集理由のメモ

---

## 4. ステータス管理

### 必須情報

- 文書(ジョブ)単位ステータス
  - **ジョブ実行ステータス**: `queued` / `running` / `succeeded` / `failed` / `canceled`
  - **レビュー状態**: `unreviewed` / `completed` / `completed_with_edit`
  - 一覧バッジで両者を別表示
- ブロック単位レビュー状態
  - `未確認` / `完了` / `修正完了`
- 一括完了
  - 現ページの未確認ブロックをまとめて確認済化
- 保存マーカー
  - `*` HasChanges、自動保存後にクリア

### 関連実装(他画面)

- レビュー状態は `IReviewStateGatewayPort` 経由でブロック単位に永続化
  - PATCH `/v1/jobs/{job_id}/review-state`
- ジョブ実行ステータスは OCR 実行画面のジョブ作成と共通

### 将来要件

- 「確認中」状態の追加 (`未確認` と `完了` の中間)
  - 現状は 2 値遷移のみ
- 承認 / 差戻しのワークフロー
  - レビュー状態に「承認待ち」「承認済み」を追加する設計
- 承認者の権限管理
  - ユーザー識別の仕組み(現状なし)とセットで設計

### 注意事項

- 文書単位の「エラー」は **ジョブ実行ステータスの `failed`** に対応
  - レビュー状態とは別軸で扱う
- レビュー状態の `completed_with_edit` は編集を伴う完了
  - 戻す操作には確認ダイアログを挟む

---

## 5. OCR 信頼度表示

### 必須情報

- 信頼度値の表示
  - 右ペインのテキストブロックにパーセント表示(`P0` フォーマット)
- 信頼度レベル分類
  - 高 (≥0.9) / 中 (≥0.7) / 低 (<0.7) / 不明
  - `Confidence` Value Object に集約(`OcrViewer.Sdk/Domain/ValueObjects/Confidence.cs`)
- bbox 色分け
  - 高: 緑 / 中: 黄 / 低: 赤 / 不明: 青
- ホバー時の TextLabel 表示
  - bbox 上にマウスを乗せるとテキストプレビューを表示

### 将来要件

- OCR 信頼度と抽出信頼度の区別
  - 現状は 1 値のみで、テンプレート抽出の信頼度は別途持たない
- 明示的な「要確認」警告バナー
  - 現状は色分けでのみ識別
- 画面側での confidence 閾値設定
  - 現状は VO 内のハードコード閾値(0.9 / 0.7)
- 低 confidence のみ絞り込み
  - 一覧の信頼度低下件数(セクション 1)と連動

### 注意事項

- ライン / テーブル単位の信頼度は持たない(`HasConfidence=false`)
  - 単語単位のみ confidence を保持
- 共通 `Confidence` VO で OCR 実行画面・テンプレート管理画面とも閾値を統一

---

## 6. テーブル認識結果

### 必須情報

- テーブル構造の取得
  - API から `NormalizedTable` / `NormalizedTableCell`(RowIndex / ColumnIndex / Text)
- テーブルのテキスト変換表示
  - `TableFormattingService.FormatAsText` で `[行,列] テキスト` 形式に整形
  - 例: `■ Table 1 (3×4)` ヘッダー + `[1,1] 日付` 等
- テーブル全体の編集
  - 1 つのテキストブロックとして編集
- Table bbox の画像オーバーレイ
  - `BoundingBoxType.Table` で表示
- 表示 ON/OFF トグル(`ShowTables`)

### 将来要件

- グリッドビュー形式の表組み表示
  - 現状はテキスト形式の縦並び
- セル単位の編集
  - 現状はテーブル全体を 1 ブロックとして編集
- 行 / 列の追加・削除
- セル結合 / 分割
  - テンプレート側に `row_span` / `col_span` 追加が必要
- 行のドラッグ並び替え
- 表構造の自動再認識

### 注意事項

- 請求書 OCR では明細部分の認識精度が業務影響に直結
- 現状の編集は 1 セルだけ修正でもテーブル全体のテキスト再入力扱いになる
  - 大規模テーブルの編集 UX 改善が課題

---

## 7. OCR 結果保存状態

### 必須情報

- 未保存マーカー
  - 右ペインのブロックに `*` HasChanges 表示
- 自動保存
  - レビュー状態変更時に API へ即時 PATCH
- 保存状態キャッシュ
  - `_reviewStateCache` で API 確定値を保持し、差分判定に使用
- 保存失敗時のロールバック
  - 失敗時は変更前の状態に戻し、ErrorMessage を表示
- 連続変更時の前回リクエスト自動キャンセル
  - `CancellationTokenSource` で 1 ブロックにつき 1 リクエストのみ進行

### 将来要件

- 最終更新時刻の UI 表示
  - 現状サーバー側で `updated_at` を保持しているが UI 未表示
- ブロック単位の「保存中...」インジケータ
  - 現状はアプリ全体の `IsLoading` のみで、保存中のブロック特定は不可

### 注意事項

- 大量修正時の保存漏れ事故防止のため、自動保存と未保存マーカーは必須
- 自動保存はレビュー状態変更が起点
  - テキスト編集のみ(`HasChanges`)では PATCH されない
  - 編集確定 + `CompletedWithEdit` への遷移で初めて保存される

---

## 8. エラー表示

### 必須情報

- エラーメッセージ表示
  - 画面下部に赤背景バナー(`ErrorMessage` バインド)
- Loading インジケータ
  - ProgressBar + 「読み込み中...」
- ジョブ失敗バッジ
  - 一覧で `failed` を赤表示
- API エラーキャッチ
  - 履歴読込 / 結果読込 / 保存の各操作で例外を捕捉
- 編集バリデーション
  - 空文字 / 空白文字のみは保存不可
- API リクエストバリデーション
  - `PatchReviewStateRequest` で `edited_text` と status の整合性チェック
    (例: `edited_text` は `CompletedWithEdit` のときのみ許可)

### 関連実装(他画面/共通基盤)

- 共通 `ErrorHandler` / `ErrorCategory`
  - 本画面では未活用、raw メッセージ表示のみ

### 将来要件

- カテゴリ別アイコン・色分け
  - 共通 `ErrorHandler` を経由した構造化表示
  - 認証 / 入力 / サーバー / タイムアウト等の区別
- 失敗箇所の bbox 強調
  - 認識失敗領域を画像上で視覚的に示す
- リトライ導線
  - ジョブ単位 / ブロック単位の再実行
- Timeout の専用処理
- 必須項目不足の表示
  - テキストブロックが項目構造を持たないため、テンプレート連動が前提

### 注意事項

- 現状の `ErrorMessage` は raw メッセージで、カテゴリ別の UI 分岐は無い
  - OCR 実行画面と同様、共通基盤未活用

---

## 9. レビュー導線

### 必須情報

- ページナビゲーション
  - 前/次ボタン
  - `←` / `→` / `PageUp` / `PageDown` キー
- 編集操作
  - `Enter` で編集確定 / `Esc` でキャンセル
  - Tab は WPF 標準のフォーカス移動
- ズーム操作
  - `Ctrl++` / `Ctrl+-` / `Ctrl+0` / `Ctrl+1`
- Undo / Redo
  - `Ctrl+Z` / `Ctrl+Shift+Z`
- 補助操作
  - `Ctrl+C` で選択テキストをコピー
  - `Esc` でフィルタパネルを閉じる
- 編集モードトグル
  - 未確認ブロックを TextBox 表示に切替
- 一括完了
  - 現ページの未確認ブロックをまとめて確認済化

### 関連実装(他画面/共通基盤)

- ヘルプパネル(`HelpPanelViewModel`)で全画面のショートカット一覧を表示
  - 結果画面のショートカットも掲載済み(`HelpPanelViewModel.cs:124-131`)

### 将来要件

- ページ跨ぎの「次の未確認へ」専用ナビ
  - 現状は同ページ内の編集モード切替のみ
- Enter 連打での連続レビュー
  - 確認 → 次のブロックへ自動フォーカス
- 自動レビュー
  - 高 confidence ブロックの自動確認済化
  - confidence 閾値の画面側設定(セクション 5 と連動)

### 注意事項

- 大量修正業務ではキーボード操作の充実度がそのまま処理速度に直結

---

## 10. 履歴・監査

### 必須情報

- 編集履歴の内部保持
  - `TextEditingViewModel.EditHistory` で `UndoRedoService` 経由の編集スタック
- Undo / Redo
  - `Ctrl+Z` / `Ctrl+Shift+Z`、ボタン Tooltip に次操作を表示
- サーバー側の更新追跡
  - `updated_at` と `edited_text` を永続化

### 将来要件

- 編集履歴の UI 表示
  - タイムライン / 一覧で過去の修正を可視化
  - 現状は内部スタックのみで、ユーザーから見えない
- 編集者の記録
  - ユーザー識別の仕組みが現状なし
  - 認証基盤とセットで設計が必要
- 修正前後の差分ビュー
- ブロック単位の最終更新時刻表示
  - サーバーは保持済、UI 未表示
- 監査ログのエクスポート
- 変更履歴のロールバック
  - 過去の編集に戻す UI
  - サーバー側に履歴スナップショットを保持する設計が前提

### 注意事項

- 現状の Undo / Redo は **セッション内のみ** で、アプリ再起動後は失われる
- ロールバック実装にはサーバー側に履歴スナップショット保存機構が必要

---

# 結果確認画面の代表的な画面分割

現在の実装状況・将来要件・AI-OCR 製品の実例を踏まえ、
結果確認画面のレイアウト案を以下の 2 軸 × 3 パターンで整理する。

- **将来設計なし版**(現実装寄り) … パターン①〜③
- **将来設計あり版**(AI-OCR 完成系寄り) … パターン④〜⑥

結果確認画面は AI-OCR において最も重要な画面であり、

```text
「OCR結果を見る画面」
```

ではなく、

```text
「OCR結果を確認・修正・承認・管理する画面」
```

として設計する必要がある。

---

## 前提整理

現在の設計は、左:履歴一覧 / 中央:原本 + bbox / 右:編集 の責務分離が非常に綺麗。

| 方針 | 評価 |
| ---- | -- |
| 左:処理履歴一覧 | ◎ |
| 中央:原本画像 + bbox | ◎ |
| 右:OCR 結果編集 | ◎ |
| confidence 色分け | ◎ |
| レビュー状態の永続化 | ◎ |
| キーボードショートカット | ◎ |

---

# 将来設計なし版(現実装ベース)

---

## パターン① シンプル3ペイン型(最推奨)

現在の構成をそのまま強化する案。

### 画面図

```text
┌────────────┬──────────────────────┬──────────────┐
│ 処理履歴一覧 │ 原本画像 + bbox       │ OCR結果編集   │
│             │                      │              │
│ invoice.pdf │ ┌────────────┐       │ 会社名       │
│ order.pdf   │ │ bbox       │       │ 金額         │
│ receipt.pdf │ └────────────┘       │ 日付         │
│             │                      │ confidence   │
│             │                      │ Status       │
│             │                      │              │
│             │                      │ [保存]       │
└────────────┴──────────────────────┴──────────────┘
```

### 画面分割

| 領域 | 内容 |
| ---- | ---- |
| 左 20% | 処理履歴一覧 |
| 中央 50% | 原本画像 + bbox |
| 右 30% | OCR 結果編集 |

### メリット

| 項目 | 評価 |
| ---- | -- |
| OCR 業務向き | ◎ |
| bbox 確認しやすい | ◎ |
| 修正効率高い | ◎ |
| 運用しやすい | ◎ |

### デメリット

| 項目 | 評価 |
| ---- | -- |
| 情報量多い | △ |
| 小画面弱い | △ |
| 初心者やや難しい | △ |

### 向いている

```text
・本命構成
・業務OCR
・大量レビュー
```

---

## パターン② 2ペイン型(DX Suite 系)

簡易レビュー向け。

### 画面図

```text
┌────────────────────┬──────────────────┐
│ 原本画像 + bbox     │ OCR結果編集      │
│                    │                  │
│ ┌────────────┐     │ 会社名           │
│ │ bbox       │     │ 金額             │
│ └────────────┘     │ 日付             │
│                    │ confidence       │
│                    │ Status           │
│                    │                  │
│                    │ [保存]           │
└────────────────────┴──────────────────┘
```

### 画面分割

| 領域 | 内容 |
| ---- | ---- |
| 左 60% | 原本画像 |
| 右 40% | OCR 編集 |

### メリット

| 項目 | 評価 |
| ---- | -- |
| シンプル | ◎ |
| 学習コスト低い | ◎ |
| 画像見やすい | ◎ |

### デメリット

| 項目 | 評価 |
| ---- | -- |
| 履歴弱い | △ |
| 大量処理弱い | △ |
| 一覧切替効率悪い | △ |

### 向いている

```text
・小規模OCR
・初心者向け
```

---

## パターン③ タブ分離型

Web SaaS 向け。

### 画面図

```text
┌────────────────────────────────┐
│ [画像] [OCR結果] [履歴] [監査] │
├────────────────────────────────┤
│                                │
│ OCR結果編集                    │
│                                │
│ confidence                     │
│                                │
│ [保存]                         │
└────────────────────────────────┘
```

### 画面分割

| 領域 | 内容 |
| ---- | ---- |
| 上部 | タブ |
| 中央 | 表示内容 |
| 下部 | 保存操作 |

### メリット

| 項目 | 評価 |
| ---- | -- |
| 小画面向き | ◎ |
| 整理しやすい | ◎ |
| Web 向き | ◎ |

### デメリット

| 項目 | 評価 |
| ---- | -- |
| bbox 確認弱い | △ |
| 操作往復増える | △ |
| OCR レビュー速度低下 | △ |

### 向いている

```text
・Web SaaS
・軽量UI
```

---

# 将来設計あり版(完成系 AI-OCR)

---

## パターン④ OCR Studio 型(UiPath 系)

最終完成系。

### 画面図

```text
┌────────────┬──────────────────────┬──────────────┐
│ 処理履歴一覧 │ OCR Preview          │ 編集詳細      │
│             │                      │              │
│ invoice.pdf │ bbox + OCR結果       │ Validation   │
│ order.pdf   │ Table Preview       │ Regex        │
│ receipt.pdf │ Confidence          │ Dictionary   │
│             │                      │ AI Suggest   │
│             │                      │ History      │
│             │                      │ Status       │
│             │                      │              │
│             │                      │ [保存]       │
└────────────┴──────────────────────┴──────────────┘
```

### 将来要件

| 機能 | 内容 |
| ---- | ---- |
| OCR Preview | OCR 確認 |
| Confidence | 信頼度表示 |
| Dictionary 連携 | 辞書補正 |
| AI Suggest | AI 修正候補 |
| History | 編集履歴 |

### メリット

| 項目 | 評価 |
| ---- | -- |
| OCR 業務最強 | ◎ |
| 修正効率高い | ◎ |
| 監査対応強い | ◎ |

### デメリット

| 項目 | 評価 |
| ---- | -- |
| UI 複雑 | △ |
| 実装コスト高い | △ |

### 向いている

```text
・OCRセンター
・高精度OCR
・監査業務
```

---

## パターン⑤ AI 補助型(Azure / Google 系)

AI レビュー支援型。

### 画面図

```text
┌────────────┬──────────────────────┬──────────────┐
│ 処理履歴一覧 │ AI bbox Preview      │ AI Suggest    │
│             │                      │              │
│ invoice.pdf │ ┌────────────┐       │ 修正候補     │
│ order.pdf   │ │ bbox       │       │ 信頼度低下   │
│ receipt.pdf │ └────────────┘       │ 辞書候補     │
│             │                      │ AI補正案     │
│             │                      │              │
│             │                      │ [適用]       │
└────────────┴──────────────────────┴──────────────┘
```

### 将来要件

| 機能 | 内容 |
| ---- | ---- |
| AI 補正 | OCR 修正提案 |
| 低 confidence 抽出 | 要確認領域抽出 |
| AI 辞書提案 | 誤認識学習 |
| 自動レビュー | 高 confidence 自動完了 |

### メリット

| 項目 | 評価 |
| ---- | -- |
| 修正速度向上 | ◎ |
| AI 感強い | ◎ |
| レビュー効率高い | ◎ |

### デメリット

| 項目 | 評価 |
| ---- | -- |
| AI 依存 | △ |
| 誤提案リスク | △ |
| UI 複雑化 | △ |

### 向いている

```text
・AI OCR
・大量帳票レビュー
```

---

## パターン⑥ Table 特化型(請求書 OCR 向け)

明細 OCR 向け。

### 画面図

```text
┌────────────┬──────────────────────┬──────────────┐
│ 処理履歴一覧 │ 明細Table Preview     │ Table編集     │
│             │                      │              │
│ invoice.pdf │ ┌────┬────┬────┐     │ Rows         │
│ order.pdf   │ │Qty │Price│Amt │     │ Columns      │
│             │ ├────┼────┼────┤     │ Header       │
│             │ │10  │100 │1000│     │ Validation   │
│             │ └────┴────┴────┘     │              │
│             │                      │ [保存]       │
└────────────┴──────────────────────┴──────────────┘
```

### 将来要件

| 機能 | 内容 |
| ---- | ---- |
| Cell 編集 | セル単位修正 |
| 行追加 | 明細編集 |
| Dynamic Row | 可変行 |
| Table 再 OCR | 表再認識 |

### メリット

| 項目 | 評価 |
| ---- | -- |
| 請求書 OCR 最強 | ◎ |
| 明細修正しやすい | ◎ |
| ERP 連携向き | ◎ |

### デメリット

| 項目 | 評価 |
| ---- | -- |
| 汎用 OCR 弱い | △ |
| UI 特化しすぎ | △ |

### 向いている

```text
・請求書OCR
・経理OCR
・ERP連携
```

